define(
    ["app"],
    function (app) {
        app.factory("SideBarService", [
            function () {
                var sidebar = {
                    toggle: function () { 
                    
                    }
                };

                return sidebar;
            }
        ]);
    });